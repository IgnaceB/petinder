import { PopularityValuePipe } from './popularity-value.pipe';

describe('PopularityValuePipe', () => {
  it('create an instance', () => {
    const pipe = new PopularityValuePipe();
    expect(pipe).toBeTruthy();
  });
});
